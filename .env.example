# ==================================================================
# CDS-EV Lab Integration Environment Configuration
# ==================================================================
# Copy this file to .env and fill in actual values
# DO NOT commit .env to version control
# ==================================================================

# ====================
# Shared Configuration
# ====================

# JWT Secret - MUST be identical for both CDS and EV Lab
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-min-32-characters

# Node Environment
NODE_ENV=integration

# ====================
# CDS Configuration
# ====================

# Database
CDS_DB_NAME=cds_integration
CDS_DB_USER=cds_user
CDS_DB_PASSWORD=change-this-secure-password-123

# Ports (different from demo environment)
CDS_FRONTEND_PORT=3010
CDS_BACKEND_PORT=3011
CDS_DB_PORT=5433
CDS_REDIS_PORT=6380

# EV Lab Integration
EV_LAB_BASE_URL=http://localhost:3020
EV_LAB_API_URL=http://localhost:8010
EV_LAB_API_KEY=change-this-32-char-api-key-abc123def456ghi789jkl012mno345pq

# ====================
# EV Lab Configuration
# ====================

# Database
EV_LAB_DB_NAME=ev_lab_integration
EV_LAB_DB_USER=ev_lab_user
EV_LAB_DB_PASSWORD=change-this-secure-password-456

# Ports (different from demo environment)
EV_LAB_FRONTEND_PORT=3020
EV_LAB_API_PORT=8010
EV_LAB_DB_PORT=5434
EV_LAB_REDIS_PORT=6381
PYBAMM_PORT=8011
EVSIM_PORT=8012
LIIONPACK_PORT=8013

# CDS Integration
CDS_BASE_URL=http://localhost:3010
CDS_BACKEND_URL=http://localhost:3011
CDS_API_KEY=change-this-32-char-api-key-xyz789abc012def345ghi678jkl901mno
CDS_WEBHOOK_URL=http://cds-backend:3001/api/integrations/lab-results
ENABLE_CDS_INTEGRATION=true

# ====================
# Development Settings
# ====================

# Logging
LOG_LEVEL=debug

# Timeouts (seconds)
PYBAMM_SIMULATION_TIMEOUT=300
EVSIM_SIMULATION_TIMEOUT=120
LIIONPACK_SIMULATION_TIMEOUT=120

# ====================
# Security Notes
# ====================
# 1. Generate unique API keys for each environment
# 2. Use strong passwords (min 16 characters, mixed case, numbers, symbols)
# 3. JWT_SECRET must be at least 32 characters
# 4. Never commit .env to version control
# 5. Rotate secrets regularly (every 90 days recommended)

# ====================
# Port Reference
# ====================
# Demo CDS:          3000 (frontend), 3001 (backend), 5432 (db), 6379 (redis)
# Demo EV Lab:       3000 (frontend), 8000 (api), 5432 (db), 6379 (redis)
# Integration CDS:   3010 (frontend), 3011 (backend), 5433 (db), 6380 (redis)
# Integration EV Lab: 3020 (frontend), 8010 (api), 5434 (db), 6381 (redis)
# Simulation Services: 8011 (PyBaMM), 8012 (EV_sim), 8013 (liionpack)
